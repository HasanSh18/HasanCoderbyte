# Search Highlight App (React + Vite)

A small search UI: type a keyword, it filters posts and **highlights** matches in titles and excerpts. No backend.

## Run locally
```bash
npm install
npm run dev
